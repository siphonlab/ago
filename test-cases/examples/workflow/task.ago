fun fail() native "org.siphonlab.ago.test.TestFunctions.quitNow";

fun rnd() as boolean native "org.siphonlab.ago.test.TestFunctions.rnd";

fun save(n as int) as int with Task {
    return n + 1;
}

fun nosave(n as int) as string {
    return "String: " + n;
}

fun showf(n as int) {
    var s = nosave(n);
    Trace.print(s);
}

fun runf() {
    var n = 10;
    showf(n);
    var x = save(n);
    if (rnd()) {
        fail();
    }
    Trace.print(x);
}
fun main() {
    runf();
}