fun add(a as int, b as int) as int{
    return a + b;
}

// sleep 10s inside, for stop/resume
fun addNative(a as int, b as int) as int native "org.siphonlab.ago.TestFunctions.add";

fun main(){
    var c = addNative(2, 3)
    Trace.print(c)
}