class Test{
    fun f(){
        var s1 = "-\u005ct-"; // -    -
        var s2 = '-\u005c"-'; // -"-
        var s3 = "-\u005c\-"; // -\-
        var s4 = "-\u005c'-"; // -'-
        var s5 = '\'\u005c"';   // -"-

        var s6 = "\u005c101"; //A
        var s7 = "\u005c043"; //#
        var s8 = "\u005c0"; // nul

        var c1 as char = '\u005cn'; // \n
        var c2 as char = '\u005c\''; // \'
        var c3 as char = '\u005c"'; // \"
        var c4 as char = '\u005c\\'; // \\

        var empty = ``;

        var simple = `foobar`;

        var lineBreak = `
        `;

        var nested = `aaa${`bbb`}ccc`;

        var str =
                `${dsName}${`( ${parameterStr} )` if parameterStr else ""}${`{
            ${returns}}` if returns else ""}`;

        var customer = { name: "Foo" }
        var card = { amount: 7, product: "Bar", unitprice: 42 }
        var message = `Hello ${customer.name},
        want to buy ${card.amount} ${card.product} for
        a total of ${card.amount * card.unitprice} bucks?`

        //------------------------------------------------------------------------------
        // Custom Interpolation
        // Flexible expression interpolation for arbitrary methods.
        // http://es6-features.org/#CustomInterpolation
        //------------------------------------------------------------------------------

        var s = `http://example.com/foo?bar=${bar + baz}&quux=${quux}`

    }
}